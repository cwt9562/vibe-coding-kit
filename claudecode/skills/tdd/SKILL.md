---
name: tdd
description: 测试驱动开发，遵循 red-green-refactor 循环。当用户想使用 TDD 构建功能或修复 bug、提到 "red-green-refactor"、需要集成测试或要求测试优先开发时触发。
---

# 测试驱动开发

## 核心理念

**核心原则**：测试应通过公共接口验证行为，而非实现细节。代码可以完全重构，但测试不应受到影响。

**好的测试**是集成风格的：它们通过公共 API 执行真实的代码路径。它们描述的是系统**做什么**，而非**怎么做**。好的测试读起来就像一份规格说明 ——"用户可以使用有效购物车结账" 准确告诉你存在什么能力。这类测试能够经受住重构的考验，因为它们不关心内部结构。

**糟糕的测试**与实现紧密耦合。它们 mock 内部协作对象、测试私有方法，或通过外部手段验证（例如直接查询数据库而不是使用接口）。警示信号：当重构时测试失败，但行为并未改变。如果你重命名了一个内部函数导致测试失败，那这些测试就是在测试实现，而非测试行为。

示例请参见 [tests.md](tests.md)，mock 指南请参见 [mocking.md](mocking.md)。

## 反模式：水平切片

**不要先把所有测试写完，再写全部实现。** 这就是"水平切片"——把 RED 阶段理解为"写所有测试"，把 GREEN 阶段理解为"写所有代码"。

这种做法会产生**糟糕的测试**：

- 批量编写的测试测试的是**想象中**的行为，而非**实际的**行为
- 最终你测试的是事物的**形状**（数据结构、函数签名），而非面向用户的行为
- 测试对真实变更变得迟钝 —— 行为被破坏时测试却通过，行为正常时测试反而失败
- 超出你的视野范围，在理解实现之前就锁定了测试结构

**正确的做法**：通过 tracer bullet 进行垂直切片。一个测试 → 一个实现 → 重复。每个测试都响应你从上一个周期中学到的东西。因为你刚刚写了代码，所以确切知道哪些行为重要以及如何验证。

```
错误做法（水平切片）：
  RED:   test1, test2, test3, test4, test5
  GREEN: impl1, impl2, impl3, impl4, impl5

正确做法（垂直切片）：
  RED→GREEN: test1→impl1
  RED→GREEN: test2→impl2
  RED→GREEN: test3→impl3
  ...
```

## 工作流程

### 1. 计划阶段

在浏览代码库时，使用项目的领域词汇表，确保测试名称和接口词汇与项目语言一致，并尊重你涉及区域的 ADR。

在编写任何代码之前：

- [ ] 与用户确认需要哪些接口变更
- [ ] 与用户确认要测试哪些行为（确定优先级）
- [ ] 识别 [deep modules](deep-modules.md) 的机会（小接口、深实现）
- [ ] 为[可测试性](interface-design.md)设计接口
- [ ] 列出要测试的行为（而非实现步骤）
- [ ] 获取用户对计划的认可

提问："公共接口应该是什么样的？哪些行为最重要，需要优先测试？"

**你不可能测试所有东西。** 与用户确认哪些行为最为关键。将测试精力集中在关键路径和复杂逻辑上，而不是每个可能的边界情况。

### 2. Tracer Bullet（探针子弹）

编写一个测试，验证系统的一个方面：

```
RED:   为第一个行为编写测试 → 测试失败
GREEN: 编写最简代码使其通过 → 测试通过
```

这就是你的 tracer bullet —— 证明整个路径可行。

### 3. 增量循环

对剩余每个行为：

```
RED:   编写下一个测试 → 失败
GREEN: 最简代码使其通过 → 通过
```

规则：

- 一次只写一个测试
- 只写足以通过当前测试的代码
- 不要预判未来的测试
- 保持测试聚焦于可观察的行为

### 4. 重构阶段

在所有测试通过后，寻找[重构候选](refactoring.md)：

- [ ] 提取重复代码
- [ ] 深化模块（将复杂性隐藏在简单接口背后）
- [ ] 在自然的地方应用 SOLID 原则
- [ ] 思考新代码揭示了关于现有代码的什么信息
- [ ] 每个重构步骤后运行测试

**在 RED 状态下绝对不要重构。** 先到达 GREEN。

## 每个周期的检查清单

```
[ ] 测试描述的是行为，而非实现
[ ] 测试只使用公共接口
[ ] 测试能够经受内部重构
[ ] 代码对于当前测试是最简的
[ ] 没有添加推测性的功能
